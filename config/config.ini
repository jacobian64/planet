# Planet configuration file

# Every planet needs a [Planet] section
[Planet]
# name: Jacobian's feeds
# link: htpp://lagi.date
# owner_name: Jacobian
# owner_email: me@jacobian.xyz
name = Planet Conundrum
link = http://lagi.date/
owner_name = Jacobian
owner_email = me@jacobian.xyz

# cache_directory: Where cached feeds are stored
# new_feed_items: Number of items to take from new feeds
# log_level: One of DEBUG, INFO, WARNING, ERROR or CRITICAL
cache_directory = cache
new_feed_items = 40
log_level = DEBUG

# template_files: Space-separated list of output template files
template_files = /home/jacoqcjo/public_html/lagi.date/config/index.html.tmpl /home/jacoqcjo/public_html/lagi.date/config/rss20.xml.tmpl /home/jacoqcjo/public_html/lagi.date/config/rss10.xml.tmpl /home/jacoqcjo/public_html/lagi.date/config/opml.xml.tmpl /home/jacoqcjo/public_html/lagi.date/config/foafroll.xml.tmpl

# The following provide defaults for each template:
# output_dir: Directory to place output files
# items_per_page: How many items to put on each page
# days_per_page: How many complete days of posts to put on each page
#                This is the absolute, hard limit (over the item limit)
# date_format: strftime format for the default 'date' template variable
# new_date_format: strftime format for the 'new_date' template variable
# encoding: output encoding for the file, Python 2.3+ users can use the
#           special "xml" value to output ASCII with XML character references
# locale: locale to use for (e.g.) strings in dates, default is taken from your
#         system
output_dir = /home/jacoqcjo/public_html/lagi.date/
items_per_page = 60
days_per_page = 15
date_format = %B %d, %Y %I:%M %p
new_date_format = %B %d, %Y
encoding = utf-8
# locale = C


# To define a different value for a particular template you may create
# a section with the same name as the template file's filename (as given
# in template_files).
#
#     [config/rss10.xml.tmpl]
#     items_per_page = 30
#     encoding = xml


# Any other section defines a feed to subscribe to.  The section title
# (in the []s) is the URI of the feed itself.  A section can also be
# have any of the following options:
# 
# name: Name of the feed (defaults to the title found in the feed)
#
# Additionally any other option placed here will be available in
# the template (prefixed with channel_ for the Items loop).  You can
# define defaults for these in a [DEFAULT] section, for example
# Planet Debian uses the following to define faces:
#
#     [DEFAULT]
#     facewidth = 64
#     faceheight = 64
#
#     [http://www.blog.com/rss]
#     face = foo.png
#     faceheight = 32
#
# The facewidth of the defined blog defaults to 64.

face = nobody.png

[http://dhiku.com/feed/]
name = dhiku
face = gil.png

[http://enda.goblogmedia.com/?feed=rss2]
name = enda
face = gil.png

[http://jacobian.xyz/life/feed/rss]
name = jack
face = ric.png

[http://yahyakurniawan.net/feed/]
name = yahya kurniawan
face = box.png

[http://enabalista.com/feed/]
name = Ena Teo
face = monica.png

[http://budiputra.com/feed/]
name = Budi Putra
face = din.png

[http://tunawisma.com/feed/]
name = wirawan
face = wir.png

[http://jacobian.xyz/word/feed/]
name = jacobian
face = bel.png

